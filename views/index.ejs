<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
    <link href="/styles/tailwind.css" rel="stylesheet" />
  </head>

  <body class="font-sans bg-gray-100">
    <div class="flex flex-col min-h-screen">
      <!-- Top Menu -->
      <header class="bg-blue-600 text-white shadow-md">
        <div class="container mx-auto flex items-center justify-between p-4">
          <!-- Left section -->
          <div class="flex items-center space-x-4">
            <h1 class="text-2xl font-bold">RPG Clicker Game</h1>
          </div>

          <!-- Center navigation -->
          <nav class="hidden md:flex flex-1 justify-center space-x-6">
            <a href="/" class="text-white hover:text-gray-200">Home</a>
            <a href="#" class="text-white hover:text-gray-200">About</a>
            <a href="#" class="text-white hover:text-gray-200">Contact</a>
            <a href="/game" class="text-white hover:text-gray-200">Game</a>
          </nav>

          <!-- Right section -->
          <div class="flex items-center space-x-4">
            <a
              href="/login"
              class="text-white bg-gray-800 hover:bg-gray-700 py-2 px-4 rounded"
              >Login</a
            >
            <a
              href="/signup"
              class="text-blue-600 bg-white border border-blue-600 hover:bg-blue-50 py-2 px-4 rounded"
              >Sign Up</a
            >
          </div>
        </div>
      </header>

      <!-- Main Content -->
      <main class="flex-1 flex flex-col items-center justify-center p-6">
        <h1 class="text-4xl font-bold mb-4">Welcome</h1>
        <% if (user) { %>
        <p class="text-lg mb-4">Hello, <%= user.account[0].username %>!</p>
        <a href="/logout" class="text-blue-600 hover:underline">Logout</a>
        <% } %>
      </main>

      <!-- Footer -->
      <footer class="bg-gray-800 text-white text-center py-4">
        <p>&copy; 2024 RPG Clicker Game. All rights reserved.</p>
      </footer>
    </div>
  </body>
</html>
